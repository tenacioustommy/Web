import React from "react";
import ReactDOM from "react-dom/client";
// import 根组件
import App from "./App";
// 将根组件渲染到id为root的dom节点上
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
